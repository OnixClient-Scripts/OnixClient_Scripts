-- if not set, the folder name will be used
name    = 'Onix'
-- list of matched words
words   = {"name=", "description=", "help_message=", "command=", "name = ", "description = ", "help_message = ", "command = "}
-- list or matched file names. `.lua`, `.dll` and `.so` only
